# 多語言支持設置完成 ✅

## 已完成的功能

### 1. 語言支持
- ✅ **繁體中文** (zh-TW) - 默認語言
- ✅ **簡體中文** (zh-CN)
- ✅ **English** (en)

### 2. 自動語言檢測
- 應用啟動時自動檢測系統語言
- 如果系統語言不在支持列表中，默認使用繁體中文

### 3. 語言切換
- 在設置頁面可以手動切換語言
- 語言設置會保存到本地存儲
- 切換語言後立即生效

### 4. 已翻譯的內容
- ✅ 首頁所有文字
- ✅ 設置頁面所有文字
- ✅ 清單項目操作提示
- ✅ 通知設置
- ✅ 權限描述
- ✅ 關於應用信息

## 文件結構

```
src/
├── locales/
│   ├── index.ts          # i18n 配置和初始化
│   ├── zh-TW.ts          # 繁體中文語言包
│   ├── zh-CN.ts          # 簡體中文語言包
│   └── en.ts             # 英文語言包
```

## 使用方法

### 在組件中使用翻譯

```typescript
import {useTranslation} from 'react-i18next';

const MyComponent = () => {
  const {t} = useTranslation();
  
  return (
    <Text>{t('home.title')}</Text>
  );
};
```

### 在非組件中使用翻譯

```typescript
import i18n from '@locales/index';

const message = i18n.t('settings.title');
```

### 切換語言

```typescript
import {useAppStore} from '@store/useAppStore';

const {updateLanguage} = useAppStore();

// 切換到簡體中文
updateLanguage('zh-CN');

// 切換到英文
updateLanguage('en');

// 切換回繁體中文
updateLanguage('zh-TW');
```

## 語言鍵值結構

所有翻譯鍵值都遵循以下結構：

```
common.*          # 通用文字（確認、取消、刪除等）
app.*             # 應用信息
home.*            # 首頁相關
settings.*        # 設置頁面相關
  notification.*  # 通知設置
  account.*       # 帳號權限
  about.*         # 關於應用
  language.*      # 語言設置
item.*            # 清單項目相關
notification.*    # 通知相關
defaultItems.*    # 默認清單項目
```

## 添加新語言

1. 在 `src/locales/` 目錄創建新的語言文件（例如 `ja.ts`）
2. 複製現有語言文件的結構並翻譯內容
3. 在 `src/locales/index.ts` 中：
   - 導入新語言文件
   - 添加到 `resources` 對象
   - 添加到 `supportedLanguages` 數組
4. 在 `src/types/index.ts` 中更新 `SupportedLanguage` 類型

## 注意事項

1. **默認語言**：繁體中文 (zh-TW)
2. **回退語言**：如果某個翻譯鍵值缺失，會回退到繁體中文
3. **語言持久化**：語言設置會自動保存到 AsyncStorage
4. **即時切換**：切換語言後界面會立即更新，無需重啟應用

## 技術實現

- **i18next**: 核心國際化庫
- **react-i18next**: React 集成
- **expo-localization**: 系統語言檢測
- **Zustand Store**: 語言狀態管理
- **AsyncStorage**: 語言設置持久化

---

**多語言功能已完全集成，可以在設置頁面切換語言！** 🌍


